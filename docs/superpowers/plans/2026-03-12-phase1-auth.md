# Phase 1: Auth Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить обязательную авторизацию (email + пароль) через Supabase Auth без потери существующих данных участников.

**Architecture:** Supabase Auth (`@supabase/ssr`) управляет сессиями через cookies. Middleware защищает роуты `/room/*` и `/profile`. При первом входе клиент отправляет `participantIds` из localStorage на `/api/auth/claim` — сервер привязывает существующие записи `Participant` к `userId`. Новая таблица `Profile` хранит имя пользователя.

**Tech Stack:** `@supabase/supabase-js`, `@supabase/ssr`, Prisma 7, Next.js 14 App Router, Tailwind CSS, Material Symbols Outlined

---

## Chunk 1: Окружение и схема данных

### Task 1: Установить зависимости

**Files:**
- Modify: `package.json`

- [ ] Установить пакеты:
```bash
npm install @supabase/supabase-js @supabase/ssr
```

- [ ] Убедиться что установились без ошибок:
```bash
cat package.json | grep supabase
```
Ожидаемый вывод: строки с `@supabase/supabase-js` и `@supabase/ssr`

- [ ] Commit:
```bash
git add package.json package-lock.json
git commit -m "chore: add supabase auth dependencies"
```

---

### Task 2: Переменные окружения

**Files:**
- Modify: `.env`

- [ ] Добавить в `.env` (значения брать из Supabase Dashboard → Settings → API):
```
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

- [ ] Убедиться что переменные доступны в runtime:
```bash
node -e "require('dotenv').config(); console.log(process.env.NEXT_PUBLIC_SUPABASE_URL ? 'OK' : 'MISSING')"
```

> **Примечание:** `NEXT_PUBLIC_*` переменные видны на клиенте — это корректно для Supabase anon key (он публичный по дизайну, доступ контролируется RLS).

---

### Task 3: Prisma schema — Profile + Participant.userId

**Files:**
- Modify: `prisma/schema.prisma`

- [ ] Добавить модель `Profile` и поле `userId` в `Participant`:
```prisma
model Profile {
  id        String   @id
  email     String   @unique
  name      String
  createdAt DateTime @default(now())
}

model Participant {
  id        String    @id @default(cuid())
  roomId    String
  name      String
  userId    String?   // nullable: существующие записи не имеют userId
  createdAt DateTime  @default(now())
  room      Room      @relation(fields: [roomId], references: [id], onDelete: Cascade)
  sessions  Session[]
}
```

- [ ] Создать и применить миграцию:
```bash
npx prisma migrate dev --name add_auth_profile
```
Ожидаемый вывод: `The following migration(s) have been applied`

- [ ] Проверить что схема применилась (откроется Prisma Studio):
```bash
npx prisma studio
```
Убедиться что таблица `Profile` появилась и у `Participant` есть колонка `userId`.

- [ ] Commit:
```bash
git add prisma/schema.prisma prisma/migrations/
git commit -m "feat: add Profile model and Participant.userId for auth"
```

---

## Chunk 2: Supabase клиенты и Middleware

### Task 4: Создать Supabase server-клиент

**Files:**
- Create: `lib/supabase-server.ts`

- [ ] Создать файл `lib/supabase-server.ts`:
```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createClient() {
  const cookieStore = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {
            // Server Component — cookies read-only, ignore
          }
        },
      },
    }
  )
}
```

---

### Task 5: Создать Supabase browser-клиент

**Files:**
- Create: `lib/supabase-client.ts`

- [ ] Создать файл `lib/supabase-client.ts`:
```typescript
import { createBrowserClient } from '@supabase/ssr'

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

---

### Task 6: Middleware для защиты роутов

**Files:**
- Create: `middleware.ts`

- [ ] Создать `middleware.ts` в корне проекта:
```typescript
import { createServerClient } from '@supabase/ssr'
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

export async function middleware(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll()
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value)
          )
          supabaseResponse = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
        },
      },
    }
  )

  const { data: { user } } = await supabase.auth.getUser()

  const pathname = request.nextUrl.pathname
  const isProtected =
    pathname.startsWith('/room/') || pathname.startsWith('/profile')
  const isAuthPage = pathname === '/login' || pathname === '/register'
  const isLanding = pathname === '/'

  if ((isProtected || isLanding) && !user) {
    const url = request.nextUrl.clone()
    url.pathname = '/login'
    return NextResponse.redirect(url)
  }

  if (isAuthPage && user) {
    const url = request.nextUrl.clone()
    url.pathname = '/'
    return NextResponse.redirect(url)
  }

  return supabaseResponse
}

export const config = {
  matcher: ['/', '/room/:path*', '/profile/:path*', '/login', '/register'],
}
```

- [ ] Проверить middleware вручную:
  - Открыть `http://localhost:3000/` без сессии → должен редиректить на `/login`
  - Открыть `http://localhost:3000/room/TEST` → должен редиректить на `/login`

- [ ] Commit:
```bash
git add lib/supabase-server.ts lib/supabase-client.ts middleware.ts
git commit -m "feat: add supabase clients and auth middleware"
```

---

## Chunk 3: API Routes

### Task 7: POST /api/profile — создание профиля при регистрации

**Files:**
- Create: `app/api/profile/route.ts`

- [ ] Создать `app/api/profile/route.ts`:
```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'
import { createClient } from '@/lib/supabase-server'

export async function POST(request: Request) {
  try {
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    const { name } = await request.json()
    if (!name?.trim()) {
      return NextResponse.json({ error: 'Имя обязательно' }, { status: 400 })
    }

    const profile = await prisma.profile.create({
      data: { id: user.id, email: user.email!, name: name.trim() },
    })

    return NextResponse.json(profile, { status: 201 })
  } catch (err) {
    console.error(err)
    return NextResponse.json({ error: 'Server error' }, { status: 500 })
  }
}

export async function GET() {
  try {
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    const profile = await prisma.profile.findUnique({ where: { id: user.id } })
    if (!profile) {
      return NextResponse.json({ error: 'Profile not found' }, { status: 404 })
    }

    return NextResponse.json(profile)
  } catch (err) {
    console.error(err)
    return NextResponse.json({ error: 'Server error' }, { status: 500 })
  }
}
```

---

### Task 8: POST /api/auth/claim — привязка существующих participantIds

**Files:**
- Create: `app/api/auth/claim/route.ts`

- [ ] Создать директорию и файл:
```bash
mkdir -p app/api/auth/claim
```

- [ ] Создать `app/api/auth/claim/route.ts`:
```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'
import { createClient } from '@/lib/supabase-server'

export async function POST(request: Request) {
  try {
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    const { participantIds } = await request.json()
    if (!Array.isArray(participantIds) || participantIds.length === 0) {
      return NextResponse.json({ claimed: 0 })
    }

    const result = await prisma.participant.updateMany({
      where: {
        id: { in: participantIds },
        userId: null, // только незаявленные
      },
      data: { userId: user.id },
    })

    return NextResponse.json({ claimed: result.count })
  } catch (err) {
    console.error(err)
    return NextResponse.json({ error: 'Server error' }, { status: 500 })
  }
}
```

- [ ] Commit:
```bash
git add app/api/profile/route.ts app/api/auth/
git commit -m "feat: add profile and claim API routes"
```

---

## Chunk 4: Страницы Register и Login

### Task 9: Страница регистрации /register

**Files:**
- Create: `app/register/page.tsx`

- [ ] Создать `app/register/page.tsx` (следовать дизайн-системе: Geist Mono, CSS vars, `border-radius: 0`, Material Symbols):
```typescript
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import Link from 'next/link'
import { createClient } from '@/lib/supabase-client'
import Icon from '@/components/Icon'
import ThemeToggle from '@/components/ThemeToggle'
import { useRooms } from '@/hooks/useRooms'

export default function RegisterPage() {
  const router = useRouter()
  const { rooms } = useRooms()
  const [name, setName] = useState('')
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    if (!name.trim()) return setError('введите имя')
    if (!email.trim()) return setError('введите email')
    if (password.length < 6) return setError('пароль минимум 6 символов')

    setLoading(true)
    setError('')

    try {
      const supabase = createClient()

      // 1. Создать аккаунт в Supabase Auth
      const { data, error: authError } = await supabase.auth.signUp({
        email,
        password,
      })
      if (authError) throw new Error(authError.message)
      if (!data.user) throw new Error('Ошибка регистрации')

      // 2. Создать Profile в БД
      const profileRes = await fetch('/api/profile', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ name }),
      })
      if (!profileRes.ok) {
        const d = await profileRes.json()
        throw new Error(d.error || 'Ошибка создания профиля')
      }

      // 3. Привязать существующие participantIds из localStorage
      const participantIds = rooms.map(r => r.participantId).filter(Boolean)
      if (participantIds.length > 0) {
        await fetch('/api/auth/claim', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ participantIds }),
        })
      }

      router.push('/')
    } catch (e: unknown) {
      setError(e instanceof Error ? e.message : 'ошибка')
    } finally {
      setLoading(false)
    }
  }

  return (
    <main className="min-h-dvh flex flex-col items-center justify-center p-6 bg-[var(--bg)]">
      <div className="fixed top-3 right-4 z-50">
        <ThemeToggle />
      </div>
      <div className="w-full max-w-sm">
        <div className="mb-10">
          <div className="flex items-center gap-2 mb-5">
            <img src="/icon.svg" width={20} height={20} alt="" />
            <span className="text-[10px] tracking-widest uppercase text-[var(--muted)]">
              // pushup tracker
            </span>
          </div>
          <h1 className="text-[28px] font-bold text-[var(--text)] leading-[1.15] tracking-tight">
            register()
          </h1>
          <p className="text-xs text-[var(--muted)] mt-2.5">создать аккаунт</p>
        </div>

        <form onSubmit={handleSubmit} className="flex flex-col gap-3">
          <div className="flex flex-col gap-1.5">
            <label className="text-[10px] tracking-widest uppercase text-[var(--muted)]">
              name =
            </label>
            <input
              type="text"
              placeholder="Иван"
              value={name}
              onChange={e => setName(e.target.value)}
              className="w-full px-3 py-2.5 text-sm bg-[var(--surface)] text-[var(--text)] placeholder-[var(--muted)] focus:outline-none focus:border-[#ff6b35] transition-colors"
              style={{ border: '1px solid var(--border)' }}
            />
          </div>

          <div className="flex flex-col gap-1.5">
            <label className="text-[10px] tracking-widest uppercase text-[var(--muted)]">
              email =
            </label>
            <input
              type="email"
              placeholder="ivan@example.com"
              value={email}
              onChange={e => setEmail(e.target.value)}
              className="w-full px-3 py-2.5 text-sm bg-[var(--surface)] text-[var(--text)] placeholder-[var(--muted)] focus:outline-none focus:border-[#ff6b35] transition-colors"
              style={{ border: '1px solid var(--border)' }}
            />
          </div>

          <div className="flex flex-col gap-1.5">
            <label className="text-[10px] tracking-widest uppercase text-[var(--muted)]">
              password =
            </label>
            <input
              type="password"
              placeholder="минимум 6 символов"
              value={password}
              onChange={e => setPassword(e.target.value)}
              className="w-full px-3 py-2.5 text-sm bg-[var(--surface)] text-[var(--text)] placeholder-[var(--muted)] focus:outline-none focus:border-[#ff6b35] transition-colors"
              style={{ border: '1px solid var(--border)' }}
            />
          </div>

          {error && <p className="text-[11px] text-[#ef4444]">! {error}</p>}

          <button
            type="submit"
            disabled={loading}
            className="w-full py-3 text-sm font-normal text-white bg-[#ff6b35] disabled:opacity-40 hover:opacity-85 transition-opacity mt-1"
          >
            {loading ? '// регистрируем...' : 'execute()'}
          </button>
        </form>

        <p className="text-xs text-[var(--muted)] mt-6">
          уже есть аккаунт?{' '}
          <Link href="/login" className="text-[#ff6b35] hover:underline">
            login()
          </Link>
        </p>
      </div>
    </main>
  )
}
```

---

### Task 10: Страница входа /login

**Files:**
- Create: `app/login/page.tsx`

- [ ] Создать `app/login/page.tsx`:
```typescript
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import Link from 'next/link'
import { createClient } from '@/lib/supabase-client'
import ThemeToggle from '@/components/ThemeToggle'
import { useRooms } from '@/hooks/useRooms'

export default function LoginPage() {
  const router = useRouter()
  const { rooms } = useRooms()
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    if (!email.trim()) return setError('введите email')
    if (!password) return setError('введите пароль')

    setLoading(true)
    setError('')

    try {
      const supabase = createClient()

      const { error: authError } = await supabase.auth.signInWithPassword({
        email,
        password,
      })
      if (authError) throw new Error(authError.message)

      // Привязать существующие participantIds из localStorage
      const participantIds = rooms.map(r => r.participantId).filter(Boolean)
      if (participantIds.length > 0) {
        await fetch('/api/auth/claim', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ participantIds }),
        })
      }

      router.push('/')
      router.refresh()
    } catch (e: unknown) {
      setError(e instanceof Error ? e.message : 'ошибка')
    } finally {
      setLoading(false)
    }
  }

  return (
    <main className="min-h-dvh flex flex-col items-center justify-center p-6 bg-[var(--bg)]">
      <div className="fixed top-3 right-4 z-50">
        <ThemeToggle />
      </div>
      <div className="w-full max-w-sm">
        <div className="mb-10">
          <div className="flex items-center gap-2 mb-5">
            <img src="/icon.svg" width={20} height={20} alt="" />
            <span className="text-[10px] tracking-widest uppercase text-[var(--muted)]">
              // pushup tracker
            </span>
          </div>
          <h1 className="text-[28px] font-bold text-[var(--text)] leading-[1.15] tracking-tight">
            login()
          </h1>
          <p className="text-xs text-[var(--muted)] mt-2.5">вход в аккаунт</p>
        </div>

        <form onSubmit={handleSubmit} className="flex flex-col gap-3">
          <div className="flex flex-col gap-1.5">
            <label className="text-[10px] tracking-widest uppercase text-[var(--muted)]">
              email =
            </label>
            <input
              type="email"
              placeholder="ivan@example.com"
              value={email}
              onChange={e => setEmail(e.target.value)}
              className="w-full px-3 py-2.5 text-sm bg-[var(--surface)] text-[var(--text)] placeholder-[var(--muted)] focus:outline-none focus:border-[#ff6b35] transition-colors"
              style={{ border: '1px solid var(--border)' }}
              autoFocus
            />
          </div>

          <div className="flex flex-col gap-1.5">
            <label className="text-[10px] tracking-widest uppercase text-[var(--muted)]">
              password =
            </label>
            <input
              type="password"
              placeholder="пароль"
              value={password}
              onChange={e => setPassword(e.target.value)}
              className="w-full px-3 py-2.5 text-sm bg-[var(--surface)] text-[var(--text)] placeholder-[var(--muted)] focus:outline-none focus:border-[#ff6b35] transition-colors"
              style={{ border: '1px solid var(--border)' }}
            />
          </div>

          {error && <p className="text-[11px] text-[#ef4444]">! {error}</p>}

          <button
            type="submit"
            disabled={loading}
            className="w-full py-3 text-sm font-normal text-white bg-[#ff6b35] disabled:opacity-40 hover:opacity-85 transition-opacity mt-1"
          >
            {loading ? '// входим...' : 'execute()'}
          </button>
        </form>

        <p className="text-xs text-[var(--muted)] mt-6">
          нет аккаунта?{' '}
          <Link href="/register" className="text-[#ff6b35] hover:underline">
            register()
          </Link>
        </p>
      </div>
    </main>
  )
}
```

- [ ] Commit:
```bash
git add app/register/ app/login/
git commit -m "feat: add login and register pages"
```

---

## Chunk 5: Изменения в join flow + финальная проверка

### Task 11: Обновить /api/rooms/[code]/join — использовать Profile.name

После авторизации, при входе в комнату имя берётся из `Profile`, а не вводится вручную. Но форма join на лендинге всё ещё показывает поле `your_name`. В Phase 1 нужно обновить join endpoint чтобы он принимал `userId` и брал имя из профиля автоматически.

**Files:**
- Modify: `app/api/rooms/[code]/join/route.ts`

- [ ] Обновить join endpoint:
```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'
import { createClient } from '@/lib/supabase-server'

export async function POST(
  request: Request,
  { params }: { params: Promise<{ code: string }> }
) {
  try {
    const { code } = await params
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()

    if (!user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    // Получить имя из Profile
    const profile = await prisma.profile.findUnique({ where: { id: user.id } })
    if (!profile) {
      return NextResponse.json({ error: 'Profile not found' }, { status: 404 })
    }

    const room = await prisma.room.findUnique({
      where: { code: code.toUpperCase() },
    })
    if (!room) {
      return NextResponse.json({ error: 'Комната не найдена' }, { status: 404 })
    }

    // Проверить: уже участник этой комнаты?
    const existing = await prisma.participant.findFirst({
      where: { roomId: room.id, userId: user.id },
    })
    if (existing) {
      return NextResponse.json({ ...existing, roomName: room.name })
    }

    const participant = await prisma.participant.create({
      data: {
        name: profile.name,
        roomId: room.id,
        userId: user.id,
      },
    })

    return NextResponse.json({ ...participant, roomName: room.name }, { status: 201 })
  } catch (err) {
    console.error(err)
    return NextResponse.json({ error: 'Server error' }, { status: 500 })
  }
}
```

---

### Task 12: Обновить лендинг — убрать поле `your_name` из join формы

**Files:**
- Modify: `app/page.tsx`

- [ ] В `handleJoin` убрать поле `joinName` и отправку `name`, так как имя теперь берётся из профиля на сервере:
  - Удалить `state joinName` и `setJoinName`
  - Убрать поле `your_name =` из JSX join формы
  - В `handleJoin` отправлять только код (тело запроса пустое, аутентификация через cookie)

- [ ] Также убрать из `handleCreate` отдельный запрос имени (он там не нужен был изначально).

- [ ] Обновить `POST /api/rooms` чтобы ставил `ownerId` из сессии:

**Files:**
- Modify: `app/api/rooms/route.ts`

```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'
import { createClient } from '@/lib/supabase-server'

function generateCode(): string {
  const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'
  return Array.from({ length: 6 }, () =>
    chars[Math.floor(Math.random() * chars.length)]
  ).join('')
}

export async function POST(request: Request) {
  try {
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    const { name } = await request.json()
    if (!name?.trim()) {
      return NextResponse.json({ error: 'Название комнаты обязательно' }, { status: 400 })
    }
    if (name.trim().length > 64) {
      return NextResponse.json({ error: 'Название комнаты не более 64 символов' }, { status: 400 })
    }

    let code: string
    let attempts = 0
    do {
      code = generateCode()
      attempts++
      if (attempts > 10) throw new Error('Cannot generate unique code')
    } while (await prisma.room.findUnique({ where: { code } }))

    const room = await prisma.room.create({
      data: { name: name.trim(), code, ownerId: user.id },
    })

    return NextResponse.json(room, { status: 201 })
  } catch (err) {
    console.error(err)
    return NextResponse.json({ error: 'Server error' }, { status: 500 })
  }
}
```

> **Важно:** Для этого нужно также добавить `ownerId String?` в `Room` модель (nullable для существующих комнат). Это делается в рамках Phase 2, но `POST /api/rooms` уже должен его ставить. Временно добавить `ownerId String?` в schema и накатить миграцию.

- [ ] Добавить `ownerId String?` в `Room` в `prisma/schema.prisma` и накатить:
```bash
npx prisma migrate dev --name add_room_owner
```

- [ ] Commit:
```bash
git add app/api/rooms/ app/api/rooms/\[code\]/join/ app/page.tsx prisma/
git commit -m "feat: integrate auth into join/create flow, add ownerId to Room"
```

---

### Task 13: Финальная сквозная проверка Phase 1

- [ ] Запустить dev-сервер:
```bash
npm run dev
```

- [ ] Проверить сценарии:
  1. Открыть `http://localhost:3000/` → редирект на `/login`
  2. Открыть `/register` → зарегистрироваться → попасть на `/`
  3. Выйти (очистить cookies в DevTools → Application → Cookies → Delete All)
  4. Открыть `/login` → войти → попасть на `/`
  5. Создать комнату → убедиться что `ownerId` проставился в БД (Prisma Studio)
  6. Войти в комнату → убедиться что `Participant.userId` проставился
  7. Открыть `/room/TEST` без сессии → редирект на `/login`

- [ ] Commit финальный:
```bash
git add -A
git commit -m "feat: complete Phase 1 — auth foundation"
```
