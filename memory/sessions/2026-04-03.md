# Session Log — 2026-04-03

## Skill / Dev Startup Follow-up

- Problem:
  - asking Codex to "run the stand" on this worktree still risked using the detached helper path
  - in practice, the detached `nohup` startup could reach `READY` and then disappear, leaving `127.0.0.1:3000` closed a few moments later
- Root cause:
  - the generic `dev-stability-guard` skill already said "prefer live PTY", but its quick command and helper script were still biased toward detached launch
  - the project-specific `pushuptracker-redesign-workflow` skill did not yet restate the worktree-specific constraint strongly enough
- Changes:
  - updated `/Users/buluktaev/.codex/skills/dev-stability-guard/scripts/start_dev_stable.sh`
    - added explicit `--foreground` mode
    - detached mode remains available, but no longer has to be the only scripted path
  - updated `/Users/buluktaev/.codex/skills/dev-stability-guard/SKILL.md`
    - workflow now says to keep a live PTY open for worktrees where detached `next dev` dies after `READY`
    - quick command now uses `--foreground`
    - guardrails now explicitly defer to repo memory / AGENTS when detached startup is known to be unreliable
  - updated `/Users/buluktaev/.codex/skills/pushuptracker-redesign-workflow/SKILL.md`
    - for `phase2-redesign-sync`, local stand startup is now documented as:
      - `npm run dev` in a live PTY
      - or the guard helper only in `--foreground`
- Validation:
  - `bash -n /Users/buluktaev/.codex/skills/dev-stability-guard/scripts/start_dev_stable.sh` -> OK
  - the stand was relaunched in a live PTY and confirmed reachable on `http://127.0.0.1:3000/`

## Recovery Flow Polish

- Problem:
  - `/forgot-password -> проверьте почту` still had a dead gap where the form unlocked before the route transition finished
  - on `/reset-password`, Google-generated/autofilled passwords were visually reading like placeholder text instead of a real value
  - after opening a valid recovery link, `/reset-password` could briefly flash `ошибка` between `проверяем ссылку...` and the password form
- Root cause:
  - `app/forgot-password/page.tsx` was dropping `requestLoading` before `router.push(...)`, recreating the same idle gap pattern that had already been fixed on other auth routes
  - `components/Input.tsx` colored `:-webkit-autofill` from the React-side `hasValue` state, which can lag behind the browser's own generated-password/autofill paint
  - `app/reset-password/page.tsx` was using an aggressive `2s` PKCE timeout and only polling `getSession()` after marker/event hints, so `invalid` could win a race before the late recovery session resolved
- Changes:
  - updated `app/forgot-password/page.tsx`
    - kept the initial forgot-password screen in loading until the route transition into `?sent=1...` begins
    - resend still clears loading locally because it stays on the same route
  - updated `components/Input.tsx`
    - introduced a dedicated autofill text color contract
    - browser autofill/generated passwords now render in `text-primary` instead of placeholder/secondary color
  - updated `app/reset-password/page.tsx`
    - increased the PKCE recovery validation window to `5000ms`
    - poll `supabase.auth.getSession()` directly during validation instead of waiting only on marker/event hints
    - added a resolved guard so the first successful recovery resolution wins and `invalid` cannot flash afterward
    - added one final session check before falling back to `invalid`
- Validation:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/forgot-password` -> `200 OK`
  - `curl -I http://127.0.0.1:3000/reset-password` -> `200 OK`

## Reset Password Toggle Fix

- Problem:
  - the password visibility toggle on live `/reset-password` no longer reacted to clicks
- Root cause:
  - `ResetPasswordScreen` defaulted `passwordVisible` / `confirmPasswordVisible` to `false`
  - this forced `Input` into a controlled hidden state and overrode its internal toggle state
- Changes:
  - updated `components/ResetPasswordScreen.tsx`
    - removed the `false` defaults for `passwordVisible` and `confirmPasswordVisible`
    - live `/reset-password` now leaves the shared `Input` in its normal self-managed toggle mode unless a preview explicitly controls visibility
- Validation:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/reset-password` -> `200 OK`

## Reset Password Toggle Follow-up

- Problem:
  - the tunnel/live recovery form still looked broken even after the live route stopped forcing `passwordVisible={false}`
  - review surfaces were also still partially lying because reset-password previews continued to pass explicit `false` visibility props
- Root cause:
  - `app/design-preview/previews/ResetPasswordPreview.tsx` still forced `passwordVisible={false}` / `confirmPasswordVisible={false}` in all non-`password-visible` modes
  - browser-generated/autofilled passwords on the live recovery form could exist in the DOM before React state knew about them, so the custom eye-toggle had nothing reliable to reveal or submit
- Changes:
  - updated `app/design-preview/previews/ResetPasswordPreview.tsx`
    - reset-password previews now pass `true` only for the explicit `password-visible` mode and otherwise leave visibility uncontrolled
  - updated `components/Input.tsx`
    - added DOM-to-React sync for browser-filled values
    - browser autofill/generated passwords are now pushed into the controlled input state on:
      - autofill animation start
      - focus / blur
      - password toggle mouse down
    - this keeps the shared value, custom eye-toggle, and eventual submit payload in sync on live recovery screens
- Validation:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/reset-password` -> `200 OK`
  - `curl -I http://127.0.0.1:3000/screens` -> `200 OK`

## Recovery Route Handoff

- Problem:
  - the eye-toggle still behaved differently only on the live reset-password page opened from the email link, while:
    - localhost `/reset-password` worked
    - tunnel `/screens` worked
  - this isolated the remaining bug to the recovery-link route instance itself, not to the shared `Input` or to the tunnel host globally
- Root cause:
  - the password form opened directly on top of the transient PKCE/email-link recovery route instance
  - even after the recovery session was accepted, the page could keep running in that transitional runtime instead of on a clean `/reset-password` instance
- Changes:
  - updated `app/reset-password/page.tsx`
    - introduced a one-time clean handoff after successful recovery detection from the email redirect
    - when a recovery link resolves successfully, the page now stores the recovery marker and does a hard replace to the clean `/reset-password`
    - the follow-up page mount then renders the form from a stable route instance using the existing marker/session path
- Validation:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/reset-password` -> `200 OK`
  - `curl -I https://wohafg-ip-50-7-33-234.tunnelmole.net/reset-password` -> `200 OK`

## Pre-Prod Release Pack

- Problem:
  - redesign implementation was no longer the main release risk; the bigger risk was shipping without release gates, staging/prod env rules, or an explicit contract for preview routes
  - `render.staging.yaml` still pointed at the stale `codex/phase1-auth-staging-smoke` branch
  - review routes were hidden only by `NODE_ENV === 'production'` inside client pages, which is not a reliable server-side production contract and also conflicts with staging running in production mode
  - there was no repo-local checklist, no CI workflow, no smoke harness, and no standardized release commands
- Root cause:
  - release concerns still lived mostly in chat/memory instead of executable repo artifacts
  - preview-route gating was implemented only at the page component layer, not at server/runtime boundaries
  - the repo had no standardized `typecheck`, `legacy audit`, `health smoke`, or Playwright smoke entrypoints
- Changes:
  - added `lib/review-routes.ts`
    - centralized `NEXT_PUBLIC_ENABLE_REVIEW_ROUTES` logic
  - updated review routes:
    - `app/components/page.tsx`
    - `app/components/buttons/page.tsx`
    - `app/components/icon-button/page.tsx`
    - `app/components/inputs/page.tsx`
    - `app/components/text-button/page.tsx`
    - `app/screens/page.tsx`
    - `app/design-preview/page.tsx`
    - they now read the shared review-route helper instead of duplicating raw `NODE_ENV` checks
  - updated `middleware.ts`
    - added server-side `404` enforcement for `/components`, `/screens`, `/design-preview` when review routes are disabled
    - extended matcher coverage for those review routes
    - fixed unauth redirect so `/room/[code]?tab=settings` no longer leaks `tab=settings` into the login page query
  - updated deploy/env contracts:
    - `render.yaml`
    - `render.staging.yaml`
    - `.env.example`
    - added explicit `APP_PUBLIC_URL`
    - added explicit `NEXT_PUBLIC_ENABLE_REVIEW_ROUTES`
    - aligned staging config to `pushuptracker-staging` on branch `staging`
  - added release artifacts:
    - `docs/pre-prod-release-checklist.md`
    - `docs/pre-prod-status.md`
    - `docs/pre-prod-test-plan.md`
    - `.github/workflows/preprod-gate.yml`
  - added release scripts and smoke harness:
    - `package.json` scripts:
      - `typecheck`
      - `audit:legacy`
      - `smoke:health`
      - `smoke:browser`
      - `smoke:browser:headed`
      - `preprod:static`
    - `scripts/audit-legacy.sh`
    - `scripts/smoke-health.mjs`
    - `playwright.config.ts`
    - `tests/smoke/public-routes.spec.ts`
    - `tests/smoke/authenticated-room.spec.ts`
  - fixed supporting blockers discovered while standing up the release pack:
    - `.eslintrc.json` is now `root: true` so worktree lint no longer conflicts with the parent repo config
    - corrected JSX comment-like text nodes in:
      - `app/design-preview/previews/DesignSystemLight.tsx`
      - `app/design-preview/previews/DesignSystemDark.tsx`
- Validation:
  - `npm run typecheck` -> OK
  - `npm run lint` -> OK with warnings only
  - `npm run build` -> OK
  - `npm run audit:legacy` -> OK
    - warnings remain for:
      - `LEGACY_PATH_ICONS`
      - TODO in `lib/exerciseConfigs.ts`
  - `npm run preprod:static` -> OK
  - `npx playwright install chromium` -> OK
  - local prod-like smoke via `next start`:
    - `SMOKE_BASE_URL=http://127.0.0.1:3000 npm run smoke:health` -> OK
    - `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 SMOKE_REVIEW_ROUTES_MODE=closed npm run smoke:browser` -> OK
    - result:
      - 18 passed
      - 4 skipped (`authenticated-room.spec.ts` requires staging secrets / room codes)

## Prod-Direct Rollout Adaptation

- Problem:
  - the initial release pack still assumed `staging -> production`, but the actual infra no longer has a separate staging stand
  - this made the repo itself misleading: workflow, checklist, status, and test plan all pointed to a rollout path that cannot be executed
- Root cause:
  - the first release-pack pass optimized for the safer two-step rollout by default
  - once it was confirmed that there is no staging stand, the repo-local source of truth had to be rewritten to a production-direct contract
- Changes:
  - updated `.github/workflows/preprod-gate.yml`
    - remote smoke jobs now target `PROD_BASE_URL` instead of `STAGING_BASE_URL`
    - production browser smoke now runs with `SMOKE_REVIEW_ROUTES_MODE=closed`
  - updated release docs:
    - `docs/pre-prod-release-checklist.md`
    - `docs/pre-prod-status.md`
    - `docs/pre-prod-test-plan.md`
    - release path is now explicitly `release candidate -> production`
    - checklist now requires DB snapshot, production-safe smoke account, and immediate post-deploy smoke + rollback
  - updated memory/status to move the branch goal from staging signoff to prod-direct rollout preparation
- Validation:
  - release docs/workflow no longer reference a required staging path
  - remote smoke contract is now:
    - health smoke against `PROD_BASE_URL`
    - browser smoke against `PROD_BASE_URL`
    - authenticated slices remain optional and secret-driven

## GitHub Production Smoke Contract Setup

- Problem:
  - the repo-local production release pack was ready, but GitHub still had no Actions variable/secret contract for the post-deploy smoke path
  - without that setup, the future `workflow_dispatch` run would have no production host or smoke-account credentials to use
- Root cause:
  - release workflow and smoke tests were created locally first
  - the corresponding GitHub Actions repository settings had not been provisioned yet
- Changes:
  - confirmed repo-level GitHub CLI access via `gh auth status`
  - set repository variable:
    - `PROD_BASE_URL=https://pushuptracker-oq2o.onrender.com`
  - set repository secrets:
    - `SMOKE_LOGIN_EMAIL`
    - `SMOKE_LOGIN_PASSWORD`
    - `SMOKE_ROOM_CODE`
    - `SMOKE_OWNER_ROOM_CODE`
- Validation:
  - `gh variable list --repo buluktaev/PushUpTracker` now includes `PROD_BASE_URL`
  - `gh secret list --repo buluktaev/PushUpTracker` now includes all four smoke secrets
  - remaining rollout blockers are now explicitly external:
    - the workflow file still has to reach the default branch before `workflow_dispatch` exists in GitHub
    - production DB backup/snapshot still needs to be created before release

## Production DB Backup Recipe Standardization

- Problem:
  - the first attempt at a production DB backup took too long because the terminal flow branched through `yc`, Yandex CA download, and TLS verification issues before converging on a working command
  - for future release runs, the operator should only need to supply the current DB password, not rebuild the backup procedure from scratch
- Root cause:
  - the project had no repo-specific or skill-specific canonical backup recipe
  - the environment did not have `yc` installed, the CA download path timed out, and `verify-full` / `sslrootcert=system` were not reliable locally
- Changes:
  - updated the local skill:
    - `/Users/buluktaev/.codex/skills/pushuptracker-redesign-workflow/SKILL.md`
  - added a dedicated `Production DB Backup` section with:
    - fixed production DB connection contract:
      - host `rc1a-7633vqts157enmle.mdb.yandexcloud.net`
      - port `6432`
      - db `selectywellness_app`
      - user `selectywellness_app`
    - guidance to ask the user only for the password if that contract still holds
    - canonical `pg_dump` command with:
      - `sslmode=require`
      - custom archive format
      - compression
      - `pg_restore --list` verification
- Validation:
  - a local production dump was successfully created and read back with `pg_restore --list`
  - the backup recipe now lives in a reusable skill instead of being reconstructed from chat history

## Backup Contract Moved Into Repo Instructions

- Problem:
  - keeping the production DB backup recipe only in a local Codex skill was not enough, because another agent/runtime might not have access to the same local skills directory
  - the project needed one repo-native source of truth so any agent can execute the same backup path without rediscovering it
- Root cause:
  - the first standardization pass optimized for my local tooling, not for cross-agent reproducibility
- Changes:
  - updated `AGENTS.md`
    - added a dedicated `Production DB Backup Contract` section
    - requires a pre-release DB backup by default
    - fixes the canonical `pg_dump` path:
      - host `rc1a-7633vqts157enmle.mdb.yandexcloud.net`
      - port `6432`
      - db `selectywellness_app`
      - user `selectywellness_app`
      - `sslmode=require`
    - instructs agents to ask only for the DB password if the connection contract is unchanged
    - requires `pg_restore --list` validation after dump creation
  - updated `docs/pre-prod-release-checklist.md`
    - mirrored the same terminal backup contract for human-driven release runs
- Validation:
  - backup path is now documented both:
    - in repo instructions for agents
    - in release checklist for humans
