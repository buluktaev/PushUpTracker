# 2026-03-31

## Session

- Window: redesign continuation session in `feature/phase2-redesign-sync`
- Context: ongoing Figma-to-code design system sync

## What Changed

- Introduced a repo-local memory structure for Codex:
  - `MEMORY.md`
  - `memory/status.md`
  - `memory/decisions.md`
  - `memory/sessions/YYYY-MM-DD.md`
  - `memory/templates/session.md`
- Added a Codex skill concept for project memory management
- Reworked `/components` into the main component review surface with a single-theme toggle
- Synced multiple redesign primitives and cards from Figma into code and preview surfaces

## Components / Design Sync Progress

- Tokens and foundations:
  - `app/globals.css`
  - `public/tokens.html`
  - `public/icons.html`
  - `public/components.html`
- Buttons:
  - `Button`
  - `TextButton`
  - `IconButton`
  - `Loader`
- Form/input primitives:
  - `Input`
  - `RadioButton`
- Cards/navigation primitives:
  - `ChoiceCard`
  - `SelectCard`
  - `Tab`
- Icon system:
  - `Icon`
  - `lib/iconSet.ts`

## Figma Nodes Used

- Button: `114:196`
- Text Button: `169:3142`
- Icon Button: `132:726`
- Radio Button: `166:1518`
- Choice Card: `113:642`
- Select Card: `172:8145`

## Decisions

- `border-primary-default` in light theme was updated to `var(--neutral-200, #e5e5e5)`
- Radio button active state must be rendered as a single stroked SVG circle with `stroke-width=4`, not as "ring + inner dot"
- Memory adaptation for Codex should be repo-local and date-based, not only a rolling summary

## Discoveries

- The repeated `Cannot find module './682.js'` failure was caused by dev chunk corruption in `.next`
- A key trigger for that corruption was running `npm run build` while `next dev` was still running in the same worktree
- `app/icon.svg` and `public/icon.svg` conflicted on the `/icon.svg` path and caused a separate runtime failure until the duplicate app file was removed

## Problems / Risks

- The local Next.js dev loop is still more fragile than ideal during redesign-heavy iteration
- Preview inputs still emit read-only warnings because they pass `value` without `onChange`
- Any future concurrent `build` + `dev` usage in the same worktree can reintroduce the chunk failure

## Preview / Review Surfaces

- Components preview: `http://localhost:3000/components`
- Current rule: preview one theme at a time through the page-local toggle

## Relevant Commits

- `3429399` — `feat: continue redesign system sync`

## Next Steps

1. Continue taking the next Figma node one component slice at a time
2. Prefer updating an existing component rather than creating a parallel one
3. Review each slice on `/components` before moving to the next primitive
4. Treat strange `/components` failures as dev/cache issues first, not immediately as component bugs

---

## Session Update — Codex Memory Adaptation Finalized

### Context

- Goal: adapt the `miracle-infrastructure` memory approach for Codex in a way that preserves work history by date inside the repository

### What Changed

- Replaced the old single-file redesign note with a repo-local memory system:
  - `MEMORY.md` as root index
  - `memory/status.md` as current operational snapshot
  - `memory/decisions.md` as durable rationale
  - `memory/sessions/2026-03-31.md` as dated canonical history
  - `memory/templates/session.md` as the reusable session template
- Updated `AGENTS.md` so Codex reads memory at session start and writes back at the end of significant sessions
- Added the `project-memory` skill in `~/.codex/skills/project-memory`
- Added a validator script for repo-local memory integrity

### Decisions

- The canonical historical record for this project is now the dated session log, not a single rolling summary file
- Memory stays repo-local so it is visible in git history and available to future sessions and collaborators
- Significant sessions, not every short exchange, should be written back into memory

### Discoveries

- The previous one-file `MEMORY.md` was good as a snapshot but weak as a dated historical record
- The best fit for Codex is simpler than the original pack:
  - no multi-project config needed inside the repo
  - one project, one memory tree, many dated session files

### Relevant Links

- Root index: `MEMORY.md`
- Current snapshot: `memory/status.md`
- Decisions: `memory/decisions.md`
- Validator:
  - `python3 /Users/buluktaev/.codex/skills/project-memory/scripts/validate_project_memory.py /Users/buluktaev/Documents/GitHub/PushUpTracker/.worktrees/phase2-redesign-sync`

### Next Steps

1. Keep using this structure at the end of each significant redesign/debugging session
2. Commit the memory-system files when ready so the repo history also preserves the rollout of the memory approach

---

## Session Update — Screen Redesign Workflow Locked

### Window

- 2026-03-31 17:53 MSK

### Context

- Goal: freeze the post-components redesign workflow before starting full screen implementation

### What Changed

- Locked the next redesign phase to a screen-by-screen workflow
- Recorded the workflow in project rules and project memory
- Defined that the user provides the next screen order manually via Figma links or grouped screen references

### Decisions

- The implementation unit is one screen at a time, not a whole flow in one batch
- Each screen must be completed across:
  - `light` and `dark`
  - `desktop web` and `mobile PWA`
- Components remain reviewed on `/components`, while screen work remains reviewed through `app/design-preview/page.tsx`

### Discoveries

- The current codebase already fits this workflow well because:
  - primitives have a dedicated review surface on `/components`
  - screen previews are already organized as separate frames in `app/design-preview/page.tsx`

### Risks

- If screen work starts mixing multiple screens at once, it will be harder to detect whether a mismatch belongs to a component gap or a screen-level layout issue

### Next Steps

1. Wait for the next user-selected Figma screen
2. Implement that screen across desktop + mobile and light + dark
3. Only after screen parity is stable, move to the next screen

---

## Session Update — Dedicated Screen Review Surface Added

### Context

- Goal: make built screens reviewable on a single dedicated page, similar to `/components`

### What Changed

- Added `/screens` as a dedicated screen review route with a `Light / Dark` toggle
- Extracted the existing screen preview inventory into a reusable `ScreenCatalog`
- Kept `app/design-preview/page.tsx` as the broader catalog while making `/screens` the focused review surface for ongoing screen work

### Decisions

- `/screens` is now the primary page for reviewing built screens during redesign work
- `app/design-preview/page.tsx` remains available as the expanded reference surface with both themes visible together

### Relevant Paths

- `app/screens/page.tsx`
- `app/design-preview/previews/ScreenCatalog.tsx`
- `app/design-preview/page.tsx`

### Next Steps

1. Add each newly implemented screen to the shared `ScreenCatalog`
2. Review new screen work first on `/screens`
3. Keep `/components` for primitive/component validation only

---

## Session Update — Registration Screen Geometry Locked

### Window

- 2026-03-31 19:53 MSK

### Context

- Goal: stop re-deriving reviewed spacing for the empty registration screen while continuing screen-by-screen implementation

### What Changed

- Locked reviewed geometry for the registration screen family into project memory
- Captured the currently approved spacing rules directly from the Figma review pass

### Decisions

- Registration `desktop/web` form block starts `200px` from the top edge of the viewport
- Registration `desktop/web` legal block sits `40px` from the bottom edge
- Registration `mobile` legal block sits `56px` from the bottom edge

### Rationale

- These values were explicitly verified against the Figma layout and should be treated as stable screen rules, not inferred spacing

### Relevant Surface

- Screen review: `http://localhost:3000/screens`

---

## Session Update — Registration Flow Extended Through Welcome And Name Step

### Window

- 2026-03-31 20:55 MSK

### Context

- Goal: finish the remaining registration screens from the Figma registration group and keep them aligned to the same production-shaped screen architecture

### What Changed

- Added the remaining registration-flow screens on `/screens`:
  - welcome
  - name step:
    - empty
    - required validation
    - too short
    - filled
- Reordered `/screens` so the registration flow now reads in scenario order:
  1. welcome
  2. email/password step states
  3. name step states
- Tightened registration mobile geometry to match Figma:
  - form starts `144px` from the top edge
  - form width is `327px`
- Kept the same screen shells theme-driven, so light/dark parity still comes from the shared token system

### Relevant Paths

- `app/screens/page.tsx`
- `app/design-preview/previews/RegisterPreview.tsx`
- `app/design-preview/previews/RegisterNamePreview.tsx`
- `app/design-preview/previews/WelcomePreview.tsx`

### Figma Group

- `175:8704` — `Регистрация пользователя`

### Design Context Used

- Welcome:
  - `156:17817`
  - `156:17818`
  - `156:17840`
- Name step:
  - `164:1030`
  - `164:1043`
  - `171:4791`
  - `172:4931`
  - `171:4862`
  - `172:4956`
  - `171:4723`
  - `172:4981`

### Decisions

- `/screens` is now treated as a production-shaped staging surface, not a decorative catalog
- Registration mobile geometry is locked alongside the previously locked desktop geometry

### Verification

- `npx tsc --noEmit`
- `curl -I http://127.0.0.1:3000/screens`

### Next Steps

1. Continue from registration into the next user-provided screen family
2. Preserve the same approach:
   - use system components and tokens
   - keep screens production-shaped
   - keep desktop/mobile and theme parity inside the same screen implementation

### Flow Correction

- The `Welcome` screen was corrected after review:
  - it is **not** the first registration screen
  - it comes **after email confirmation**
- `/screens` was reordered accordingly so the registration sequence now reflects the reviewed product flow rather than a guessed one.

---

## Session Update — Registration Confirmation Screens Added

### Context

- Goal: finish auth-flow details in the real routes instead of leaving them as preview-only behavior

### What Changed

- Added a production route-level loading screen for App Router transitions:
  - `components/AppLoadingScreen.tsx`
  - `app/loading.tsx`
- Mirrored the same state on `/screens`:
  - `app/design-preview/previews/AppLoadingPreview.tsx`
  - `app/screens/page.tsx`
- Added pre-auth system-theme enforcement:
  - created `components/AuthThemeSync.tsx`
  - wired it into `/login`, `/register`, `/verify-email`, `/auth/confirm`, `/welcome`, `/register/name`
- Removed manual theme switching from `/login`

### Decisions

- Pre-auth routes must follow system theme only
- App transition loading is production behavior and must also appear on `/screens`

### Verification

- `npx tsc --noEmit`
- `curl -I http://127.0.0.1:3000/register`
- `curl -I http://127.0.0.1:3000/login`
- `curl -I http://127.0.0.1:3000/screens`

### Window

- 2026-03-31 21:27 MSK

### Context

- Gap found during review: registration flow on `/screens` still skipped the explicit confirmation pages between registration submit and welcome

### What Changed

- Added email confirmation waiting screen:
  - `09 - Register Verify Email`
- Added confirmation process screens:
  - `10 - Register Confirm Pending`
  - `11 - Register Confirm Error`
- Moved welcome to follow those screens:
  - `12 - Register Welcome`
- Shifted name-step numbering so the registration flow remains sequential

### Relevant Paths

- `app/design-preview/previews/VerifyEmailPreview.tsx`
- `app/design-preview/previews/ConfirmEmailPreview.tsx`
- `app/screens/page.tsx`

### Verification

- `npx tsc --noEmit`
- `curl -I http://127.0.0.1:3000/screens`

### Result

- `/screens` now shows the registration flow as:
  1. email/password entry and validation states
  2. confirmation waiting screen
  3. confirmation in-progress / error screens
  4. welcome screen
  5. name-step screens

### Follow-up Correction

- The initial confirmation block was still incomplete for the resend lifecycle.
- Added the missing verification-email states from Figma:
  - resend available
  - attempts exceeded
- Also corrected the confirmation pending/error geometry back to the reviewed desktop/mobile sizes from Figma after an earlier oversimplification had shrunk them too much.

---

## Session Update — Real Registration Flow Switched To New Design

### Window

- 2026-03-31 22:13 MSK

### Context

- The preview flow was no longer enough: the real registration routes needed to follow the new design and the reviewed flow order.

### What Changed

- Replaced the old UI on the real `/verify-email` route with the reviewed redesign layout while keeping resend logic.
- Replaced the old UI on the real `/auth/confirm` route with the reviewed pending/error layouts while keeping OTP verification and room-claim behavior.
- Changed successful email confirmation to route into `/welcome`.
- Preserved the already-updated real `/register` route and the existing `/welcome -> /register/name` handoff.

### Relevant Paths

- `app/register/page.tsx`
- `app/verify-email/page.tsx`
- `app/auth/confirm/page.tsx`
- `app/welcome/page.tsx`
- `app/register/name/page.tsx`

### Verification

- `npx tsc --noEmit`
- `curl -I http://127.0.0.1:3000/register`
- `curl -I http://127.0.0.1:3000/verify-email`
- `curl -I http://127.0.0.1:3000/auth/confirm`
- `curl -I http://127.0.0.1:3000/welcome`

### Result

- The real registration flow now uses the new design on the key routes instead of leaving the redesign only on `/screens`.
- Welcome is now actually inserted after email confirmation in the live route sequence.

---

## Session Update — Registration Flow Runtime Checks And Environment Findings

### Window

- 2026-03-31 22:30 MSK

### Context

- Live registration testing surfaced `Failed to fetch` on `/register` and visual inconsistencies on live confirmation/welcome routes.

### What Changed

- Verified that the active worktree `.env` and `.env.test` point to `jttxbcqcqulxkcrgqhyx.supabase.co`.
- Verified that this hostname does not resolve by DNS, which explains the client-side `Failed to fetch` during signup.
- Verified that the root repo `.env` points to `kteiwuaweembjpygnwtm.supabase.co`, which does resolve and accepts auth requests.
- Restarted local dev using the root repo `.env` so the real registration flow can be exercised.
- Fixed live route background alignment:
  - global `body` now uses `bg-surface`
  - old landing route background aligned to `bg-surface`
- Fixed live confirm route copy so raw English Supabase errors no longer leak to the UI.
- Replaced fixed-height route shells with `min-h-dvh` on:
  - `/verify-email`
  - `/auth/confirm`
  - `/welcome`
  - `/register/name`

### Relevant Paths

- `app/globals.css`
- `app/page.tsx`
- `app/register/page.tsx`
- `app/verify-email/page.tsx`
- `app/auth/confirm/page.tsx`
- `app/welcome/page.tsx`
- `app/register/name/page.tsx`
- `.env`
- `.env.test`

### Verification

- `python3` DNS check for both Supabase hosts
- direct `auth/v1/signup` request against the reachable Supabase project
- `npx tsc --noEmit`
- `curl -I http://127.0.0.1:3000/register`
- `curl -I http://127.0.0.1:3000/verify-email`
- `curl -I http://127.0.0.1:3000/auth/confirm`
- `curl -I http://127.0.0.1:3000/welcome`

### Result

- The current blocker for test registration is environment configuration, not redesigned auth code.
- Live route visuals are closer to the approved system:
  - no bottom background bands from fixed-height shells
  - no raw English provider text on confirm errors

---

## Session Update — Project Skill Added For Redesign Work

### Window

- 2026-03-31 22:35 MSK

### Context

- The redesign workflow had accumulated enough project-specific rules that they needed a reusable skill instead of relying on chat memory alone.

### What Changed

- Created project-specific skill:
  - `/Users/buluktaev/.codex/skills/pushuptracker-redesign-workflow/SKILL.md`
- Added checklist:
  - `/Users/buluktaev/.codex/skills/pushuptracker-redesign-workflow/references/checklist.md`

### Skill Coverage

- where source-of-truth files live
- which worktree and branch to use
- token rules for auth screens
- icon rules and icon inventory source
- component-first and production-first implementation order
- `/components` vs `/screens` responsibilities
- registration flow order
- verification expectations

### Verification

- skill files created on disk
- memory validator run after recording the addition

### Result

- PushUpTracker redesign work now has a dedicated reusable Codex skill instead of depending on chat recall.

---

## Session Update — Post-Auth Create-Room Flow Rolled Onto Real Root Route

### Window

- 2026-04-01 00:20 MSK

### Context

- The next redesign block moved from auth flow into the post-auth create-room flow on `/`.
- The requirement was to apply the reviewed design to the real product route first, then mirror the same states on `/screens`.

### What Changed

- Added shared production-first create-room screen components:
  - `components/CreateRoomFlowScreens.tsx`
- Confirmed `join` now exists as a project icon in `lib/iconSet.ts`.
- Reworked the real `/` route:
  - replaced the old no-room create flow UI with the new step sequence
  - action choice step
  - discipline choice step
  - room-name step
  - kept room creation on the existing `/api/rooms` contract
  - kept room joining on the existing `/api/rooms/[code]/join` contract
  - preserved the existing-room list branch for users who already have rooms saved locally
- Added mirrored QA states for the create-room flow on `/screens` via:
  - `app/design-preview/previews/CreateRoomPreview.tsx`
  - `app/screens/page.tsx`

### Relevant Paths

- `components/CreateRoomFlowScreens.tsx`
- `app/page.tsx`
- `app/design-preview/previews/CreateRoomPreview.tsx`
- `app/screens/page.tsx`

### Verification

- `npx tsc --noEmit`
- `curl -I -s http://127.0.0.1:3000/`
- `curl -I -s http://127.0.0.1:3000/screens`

### Result

- The redesigned create-room flow is now implemented on the real root route instead of being preview-only.
- `/screens` now mirrors the same create-room states using the same shared screen code.
