# 2026-04-02

- Исправлен `auth/confirm`: верхний `gap-2` оставлен только между бренд-строкой и группой заголовка, а `h1 + body` собраны в один внутренний контейнер без внешнего `gap`.
- Тот же паттерн выровнен в `ConfirmEmailPreview`.
- Зафиксировано долговечное правило: на auth/onboarding и create/join flow экранах `h1 + caption/body` всегда считаются одной визуальной группой; внешний `gap-2` между ними запрещен.
- Устранен flash light theme на refresh: инициализация темы перенесена в inline-script внутри `<head>` в root layout, а не подключается из `body` после первого paint.
- Выровнена механика выбора действия на post-auth entry: и при `0 rooms`, и в ветке `?add=1` выбор карточки теперь только выделяет пункт и показывает кнопку `Продолжить`; переход на следующий шаг происходит только по кнопке.
- `ChoiceCard` приведен к Figma-контракту `default / hovered / selected`: добавлен живой hover-state внутри системного компонента и синхронизация `RadioButton` с этим состоянием.
- `SelectCard` возвращен к Figma-контракту `default / hovered / selected`: живой hover снова включен, а preview-секция компонента снова показывает `hovered`.
- Исправлена навигация `room -> add_room() -> /?add=1`: исходная комната теперь пробрасывается как `fromRoom`, и back на первом add-room экране возвращает именно в нее, а не в ветку выбора комнат на `/`.
- Исправлен back по всему add-room flow при входе из комнаты: если есть `fromRoom`, возврат с action / discipline / name / join шагов ведет обратно в исходную комнату.
- Скорректирована add-room back-навигация до level-based поведения: возврат в исходную комнату оставлен только на первом экране выбора действия, а внутренние шаги (`discipline`, `name`, `join`) теперь возвращают на предыдущий шаг flow.
- Исправлен баг залипания `hovered` у `ChoiceCard`: невыбранная карточка теперь сбрасывает локальный hover-state при смене выбора в группе и на клике.
- Добавлен глобальный слой типографического wrap-полиша:
  - `h1`-`h4` теперь используют `text-wrap: balance`
  - `p`, `li`, `figcaption`, `blockquote` теперь используют `text-wrap: pretty`
- Подтверждено, что `make text crispy` уже был корректно включен глобально через сглаживание шрифтов и `text-rendering` в `app/globals.css`; повторно раскатывать его по компонентам не потребовалось.
- Для auth/create/join экранов добавлен reusable `RevealSection` и CSS section-reveal анимация:
  - stagger по semantic sections вместо анимации всего контейнера
  - подключено на `/register`, `/login`, `/register/name`, `/verify-email` и в shared `CreateRoomFlowScreens`
- В `Input` для password toggle добавлена contextual icon animation без библиотеки:
  - обе иконки глаза теперь остаются в DOM
  - переключение идет через `opacity + scale + blur`
  - логика поля и accessibility не менялись
- Зафиксировано завершение form-phase. В этот закрытый этап вошло:
  - реализация и доводка реальных auth routes:
    - `/register`
    - `/verify-email`
    - `/auth/confirm`
    - `/welcome`
    - `/register/name`
    - `/login`
  - реализация post-auth entry flow на `/`:
    - `0 rooms`
    - `1 room -> redirect`
    - `2+ rooms`
    - `?add=1`
  - синхронизация create/join экранов с Figma на live routes и `/screens`
  - унификация правил геометрии форм:
    - `logoWrapper`
    - `headingWrapper`
    - `inputWrapper`
    - `buttonWrapper`
    - `signoutWrapper`
    - правило `h1 + caption/body = одна группа`
  - исправления theme/runtime:
    - pre-auth только от системной темы
    - inline theme init в `<head>`
    - устранение flash light theme на refresh
  - унификация loading UX:
    - route-level loading screen
    - отключение интерактива во время загрузки
    - устранение dead-gap перед route transition
  - доводка карточек и entry-navigation:
    - `ChoiceCard`
    - `SelectCard`
    - `add_room()` c `fromRoom`
    - level-based back внутри add-room flow
  - типографический и motion polish:
    - global crisp text rendering
    - global `text-wrap`
    - section-level stagger reveal
    - contextual icon transition у password toggle
- Следующий этап после завершения form-phase: переход на room screens и room interactions.
- В текущем worktree создан локальный `AGENTS.md`, который дополняет корневой файл и фиксирует именно устойчивые правила, накопленные в `phase2-redesign-sync`:
  - production-first rollout
  - screen-by-screen workflow
  - form/auth/create-join geometry
  - loading/theme/navigation contracts
  - `ChoiceCard` / `SelectCard` / `Input` contracts
  - typography and motion rules
  - room-originated add-room navigation behavior
- Для тестовой Prisma DB добавлен repo-local entrypoint `npm run studio:test`, который:
  - берет `.env.test` из текущего worktree
  - выставляет TLS-relaxed запуск для Yandex Cloud
  - поднимает Prisma Studio на локальном порту без ручной сборки команды
- Для dev-стенда добавлен repo-local entrypoint `npm run dev`, который теперь:
  - копирует test env в worktree при необходимости
  - чистит stale `.next`
  - освобождает порт только от stale next-процессов
  - запускает реальный `prisma migrate deploy && next dev` в foreground
- Отдельно подтверждено, что detached `nohup`-запуск через внешний guard-script может дать ложный `READY`: сервер реально отвечает, но затем исчезает без app-level ошибки, поэтому для этого проекта он не считается надежным стандартом запуска
- Начата синхронизация room workout screen с Figma:
  - в `ExerciseConfig` добавлены короткие `tabLabel` для room workout tab
  - добавлен общий helper room-tab metadata, который реальный route и preview используют одинаково
  - static `Тренировка` в room tabs заменена на discipline-driven icon + label
  - подпись дисциплины рядом с названием комнаты в header убрана
  - mobile room tabbar приведен ближе к Figma по иконке/тексту/padding
- Продолжена синхронизация workout screen с Figma `175:10842`:
  - `CameraWorkout` и preview `CameraPreview` приведены ближе к reviewed off-state
  - статусный бейдж переведен на русский и стилизован как warning badge из Figma
  - CTA `включить камеру` переведен с legacy `enable_camera()` на Figma-copy и Figma-геометрию
  - при выключенной камере убран placeholder-низ с невидимым control row, чтобы off-state совпадал с reviewed экраном
- После review feedback по реальному `/room/[code]` доведена shell-часть workout screen:
  - root route переведен на `bg-[var(--bg-surface)]`
  - header переведен на `bg-[var(--surface)]`
  - room title оставлен без подписи дисциплины и приведен к 18/26
  - room switcher и theme toggle в header переведены на compact icon-button contract
  - desktop/mobile tabbars переведены с route-local кнопок на shared `Tab`
- `ThemeToggle` расширен режимом `iconOnly compact`, чтобы room header мог использовать Figma-style icon button, не ломая legacy text-toggle на остальных экранах.
- `IconButton` расширен компактным размером `32x32`, чтобы можно было соблюсти header geometry из Figma без отдельного дублирующего компонента.
- Исправлена логика нижнего control row в `CameraWorkout` и `CameraPreview`:
  - при включенной камере и idle теперь рендерится только `Начать сессию`
  - во время countdown рендерится только `Отмена`
  - во время active/hold/saving рендерится только danger-control `Закончить сессию`
  - legacy copy вида `start_session() / cancel() / finish() · 0 reps` убрана из reviewed surface
- `/design-preview` снова приведен к parity с реальным route по shell:
  - preview root переведен на `bg-[var(--bg-surface)]`
  - preview header переведен на `bg-[var(--surface)]`
  - fixed switcher dropdown offset синхронизирован с route
- Проверки после room workout правок:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview#workout` -> `200 OK`
  - `python3 /Users/buluktaev/.codex/skills/project-memory/scripts/validate_project_memory.py /Users/buluktaev/Documents/GitHub/PushUpTracker/.worktrees/phase2-redesign-sync` -> OK
- Уточнен responsive contract camera-frame для workout screen:
  - production `CameraWorkout` и preview `CameraPreview` теперь всегда живут в одном aspect-ratio box
  - mobile -> `3:4`
  - `sm+` / web -> `4:3`
  - width ограничен `max-w-[1024px]`, что автоматически дает desktop ceiling `1024x768`
  - убран fixed off-state height `500px`, чтобы frame не прыгал между camera-off и camera-on
- По следующему review feedback уточнена геометрия off-state workout screen:
  - CTA `включить камеру` в production и preview теперь имеет фиксированную высоту `72px`
  - workout-tab container получил явный нижний отступ от tabbar:
    - mobile -> `24px`
    - `sm+` / web -> `16px`
- Выяснено, что предыдущая попытка с внутренним `pb-6 sm:pb-4` не давала эффекта из-за `justify-center` на workout `main`.
- Workout-shell в route и preview переведен с `justify-center` на `justify-end`, а нижний отступ перенесен на сам `main`, чтобы camera frame реально якорился к низу layout:
  - mobile -> `24px`
  - `sm+` / web -> `16px`
- Следующим feedback-циклом уточнено, что нужный spacing должен быть не снизу, а сверху под header.
- Финальный workout-shell spacing contract для текущего этапа:
  - mobile -> `pt-6` (`24px`)
  - `sm+` / web -> `pt-4` (`16px`)
  - route и preview больше не используют нижний inset для этого экрана
- Реализован следующий Figma-срез workout screen: `401:18467` (`Камера включена`), но только для состояния `cameraOn + session idle`.
- В `CameraWorkout` и `CameraPreview` для idle camera-on state:
  - status chip зафиксирован как warning-state `займите горизонтальное положение`
  - top-right control приведен к Figma-copy и Figma-surface style: `отключить камеру`
  - `camera-wrapper` ограничен `max-w-[1024px]`, чтобы нижний control bar и frame были одной ширины на web
  - стартовый control bar приведен к Figma:
    - `height: 40px`
    - `background: var(--accent-default)`
    - copy `начать сессию`
  - gap между frame и нижним bar синхронизирован с Figma:
    - mobile -> `16px`
    - `sm+` / web -> `8px`
  - mobile side insets для нижнего bar -> `24px`
- Этот проход сознательно не менял countdown/active/finish состояния; они остаются следующим отдельным срезом room workout screen.
- Проверки после sync узла `401:18467`:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview#workout` -> `200 OK`
- Введен app-specific responsive contract для продукта:
  - `app-mobile` -> `<1024px`
  - `app-web` -> `>=1024px`
- Вместо глобальной ломки стандартных Tailwind `screens` продукт переведен на новые app-breakpoint aliases:
  - auth/onboarding routes
  - shared create/join shell
  - room route `/room/[code]`
  - `RoomPreview` / `CameraPreview`
  - `CameraWorkout`
  - default branch `ThemeToggle`
- Это убрало смешанный responsive split, где часть приложения жила по `sm` (`640px`), а часть по `max-md` (`768px`).
- Проверки после ввода `1024px` contract:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
  - `curl -I http://127.0.0.1:3000/login` -> `200 OK`
  - `rg` по product-layer больше не находит старые `sm:` / `max-md` в затронутых product files
- Следующим feedback-циклом по `/room/[code]` на mobile layout добавлен scroll-container у screen content:
  - `main` в room route и `RoomPreview` теперь использует `overflow-y-auto`
  - mobile bottom padding `24px` (`pb-6`) добавлен на сам content area
  - `app-web` отключает этот scroll/padding override
- Причина: sticky mobile tabbar съедал нижний visual gap между workout control bar и tabbar; layout-level scroll + bottom inset делают этот `24px` устойчивым на ограниченной высоте viewport.
- Уточнен color-token contract для workout status chip:
  - фон chip в `CameraWorkout` и `CameraPreview` теперь всегда `var(--surface)`
  - semantic state переносится только в цвет точки и текста
  - текущий mapping:
    - camera off -> accent
    - init / searching / `займите положение` -> warning
    - success/ready -> success
- Проверки после token-уточнения:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview#workout` -> `200 OK`
- Реализован следующий Figma-срез workout screen: `402:19164` (`Отсчет времени`) для countdown-state.
- В `CameraWorkout` и `CameraPreview` countdown-state теперь приведен ближе к Figma:
  - reverse counter использует `Font Family Secondary`
  - typography counter: `72px / 80px`
  - countdown cancel bar:
    - `height: 40px`
    - `background: var(--surface)`
    - `border: var(--border-primary-default)`
    - copy `отмена`
- Этот проход сознательно не менял idle start-state и active/finish states.
- Проверки после sync узла `402:19164`:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview#workout` -> `200 OK`
- Реализован следующий Figma-срез workout screen: `402:19446` (`Сессия идет`) для active-state.
- В `CameraWorkout` и `CameraPreview` active-state теперь приведен ближе к Figma:
  - active counter использует `Font Family Secondary`
  - typography counter: `72px / 80px`
  - elapsed timer использует `Font Family Secondary` с `16px / 24px`
  - active finish control приведен к Figma bar:
    - `height: 40px`
    - `background: var(--accent-default)`
    - copy `закончить сессию`
  - при этом механику завершения сессии не меняли:
    - `pointer hold` и progress-fill остались прежними
- Проверки после sync узла `402:19446`:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview#workout` -> `200 OK`
- Следующим feedback-pass для active-state уточнен текстовый и behavioral contract:
  - copy active finish bar переведен в lowercase:
    - `закончить сессию`
    - `удерживайте...`
  - horizontal warning copy нормализован:
    - web -> `займите горизонтальное положение`
    - mobile -> `займите гор. положение`
  - `отключить камеру` скрывается во время active session и saving, чтобы не оставлять running session без tracking
- Проверки после active-state feedback-pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview#workout` -> `200 OK`
- Следующим feedback-pass скорректирована mobile-геометрия workout screen:
  - верхний отступ camera frame под header на mobile снижен с `24px` до `16px`
  - mobile horizontal inset нижнего control bar снижен с `24px` до `16px`
  - те же значения зеркально обновлены в `RoomPreview` и `CameraPreview`
- Проверки после mobile-geometry feedback-pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview#workout` -> `200 OK`
- Реализован следующий экран room-flow: leaderboard / `Рейтинг` по Figma `175:14182`.
- В route `/room/[code]` и `RoomPreview` leaderboard переведен с legacy layout на Figma-list contract:
  - удалены stats bar и `refresh()`
  - список теперь состоит из чистых `ratingRow` высотой `60px`
  - mobile width -> `343px`
  - desktop width -> `720px`
  - строка рейтинга вынесена в shared `RoomLeaderboard`
  - typography строки:
    - name -> `16px / 24px` primary
    - caption -> `12px / 18px` secondary mono
    - score -> `16px / 24px` secondary mono
- Для rank marker:
  - top-3 оставлены через crown color coding
  - остальные места показываются mono rank number
- В текущем API нет данных для честного `стрик: N дней`, поэтому streak-caption в этот проход не активировался, несмотря на наличие такого optional state в Figma component.
- Проверки после sync узла `175:14182`:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Следующим feedback-pass для room tabs убран active underline:
  - shared `Tab` больше не рисует нижнюю линию у активного desktop tab
  - active-state остается только через accent color текста и иконки
- Для локального тестирования leaderboard на реальном `/room/FR2LAG` добавлен временный dev-only data override в `GET /api/rooms/[code]`:
  - только вне production
  - только для `FR2LAG`
  - добавляет 12 синтетических участников
  - `stats` пересчитываются уже по augmented leaderboard
- Проверки после tab/data feedback-pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Следующим feedback-pass для leaderboard container добавлен внешний stroke у всей группы рейтинга:
  - border вынесен в shared `RoomLeaderboard`, а не в route wrapper
  - внутренние row separators оставлены без изменений
- Проверки после leaderboard-border feedback-pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Следующим leaderboard feedback-pass убран лишний нижний separator у последней строки:
  - `RoomLeaderboardRow` теперь получает `isLast`
  - `border-bottom` снимается только у последнего row, внешний stroke контейнера остается
- Проверки после leaderboard-last-row feedback-pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Следующим style-pass для leaderboard доведен typography contract страницы `175:14182`:
  - name row text теперь явно использует `Font Family Primary`, `16px / 24px`, `400`
  - caption и score оставлены на `Font Family Secondary` с явным `letterSpacing: 0`
  - rank marker для non-top-3 тоже закреплен на secondary mono `12px / 18px`
  - current-user row приведен к Figma-state:
    - background -> `bg/primary`
    - label -> `Вы`
- Проверки после leaderboard-typography style-pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Следующим global typography pass устранен ложный font fallback по всему приложению:
  - root `app/layout.tsx` теперь реально подключает `Google Sans` и `IBM Plex Mono` через один Google Fonts stylesheet
  - `globals.css` дополнительно заставляет `button/input/textarea/select` наследовать активный шрифт
  - это закрывает расхождение, из-за которого `Font Family Secondary` визуально падал в `SFMono-Regular`
- Проверки после global-font pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/login` -> `200 OK`
  - HTML `/login` теперь содержит combined stylesheet:
    - `Google Sans`
    - `IBM Plex Mono`
- Для удобной проверки scroll на leaderboard dev-only augmentation для `FR2LAG` расширен еще на 10 synthetic rows:
  - суммарный leaderboard для route now returns `23` positions
  - это изменение по-прежнему ограничено:
    - только вне production
    - только для `FR2LAG`
- Проверки после leaderboard-scroll data pass:
  - `npx tsc --noEmit` -> OK
  - `GET /api/rooms/FR2LAG` -> `leaderboardCount: 23`
- Следующим leaderboard spacing feedback-pass добавлен desktop bottom inset для конца длинного списка:
  - на leaderboard-ветке route и `RoomPreview` теперь есть `56px` нижнего пространства при `app-web`
  - правка сделана на уровне screen layout, а не внутри shared `RoomLeaderboard`
- Проверки после leaderboard-bottom-spacing pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Следующим global mobile-shell pass изменен form/auth/create-join contract:
  - mobile side paddings reduced from `24px` to `16px`
  - shared create/join `StepContainer` now uses `w-[calc(100%-32px)]`
  - create/join back button anchor on mobile is now `left 16px / top 16px`
  - the same `16px` contract is now applied across live routes:
    - `/login`
    - `/register`
    - `/register/name`
    - `/verify-email`
    - `/auth/confirm`
    - `/welcome`
  - parity previews were updated to the same mobile width contract (`343px` in fixed mobile previews)
- Проверки после global mobile-shell pass:
  - `rg` no longer finds old product-shell values:
    - `calc(100%-48px)`
    - `w-[327px]`
    - mobile `24px` left/right anchors in auth/create/join surfaces
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/screens` -> `200 OK`
- Следующим feedback-pass исправлено залипание accent border на mobile `ChoiceCard`:
  - проблема оказалась не в `SelectCard`, а в shared `ChoiceCard`
  - hover-state теперь активируется только для `pointerType === mouse`
  - touch/coarse interactions больше не могут оставлять старую карточку в local `hovered`
  - добавлен более жесткий reset interaction state через `pointer leave / pointer up / blur`
- Проверки после `ChoiceCard` touch-hover fix:
  - `npx tsc --noEmit` -> OK
- Следующим leaderboard text-format pass локализован row copy и rank formatting:
  - subtitle теперь пишет по-русски:
    - `сессий: N`
    - `лучшая: X`
  - rank marker ниже top-3 теперь zero-padded до двух символов:
    - `04`, `05`, `06` ...
  - crowns для top-3 и score справа не менялись
- Проверки после leaderboard text-format pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Следующим room-shell mobile spacing pass общий нижний inset в `/room/[code]` снижен с `24px` до `16px`:
  - `main` route now uses `pb-4` instead of `pb-6`
  - тот же контракт зеркально применен в `RoomPreview`
  - desktop `app-web:pb-0` не менялся
- Проверки после room-shell mobile spacing pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Следующим mobile leaderboard width pass убран фиксированный mobile cap `343px`:
  - leaderboard wrapper на mobile теперь `w-full`
  - desktop cap `app-web:max-w-[720px]` сохранен
  - это позволяет списку реально растягиваться на весь доступный shell с `16px` inset
- Проверки после mobile leaderboard width pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Реализован следующий экран room-flow: profile / `Профиль` по Figma `179:5065`.
- В route `/room/[code]` и `RoomPreview` legacy profile UI заменен на shared `RoomProfilePanel`:
  - убраны debug-copy и кнопки:
    - `// profile`
    - `// account`
    - `logout()`
    - `leave_room()`
  - info block теперь следует Figma contract:
    - desktop -> `2x2` grid
    - mobile -> stacked info cells
    - background -> `bg/primary`
  - logout block теперь использует Figma copy:
    - base -> `Выйти из профиля`
    - confirm -> `Да, выйти` / `Нет, остаться`
  - member danger zone теперь использует Figma copy:
    - base -> `Покинуть комнату`
    - confirm -> `Да, покинуть` / `Нет, остаться`
  - owner-specific leave action сознательно не показан, потому что текущий backend contract запрещает owner покидать собственную комнату
  - для profile-tab снят общий `max-w-2xl` cap, чтобы shared block мог реально дотянуться до reviewed desktop width `720px`
- Проверки после profile sync pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Реализован следующий экран room-flow: settings / `Настройки` по Figma `179:8135`.
- В route `/room/[code]` и `RoomPreview` legacy owner-settings UI заменен на shared `RoomSettingsPanel`:
  - убраны debug-copy и действия:
    - `// переименовать комнату`
    - `// участники`
    - `// danger zone`
    - `kick()`
    - `delete_room()`
  - для settings-tab снят общий `max-w-2xl` cap, чтобы owner screen реально мог занять reviewed width `720px`
  - top `roomNameWrapper` теперь следует Figma rename layout:
    - labeled room-name input
    - `Сохранить`
    - save остается disabled, пока значение реально не изменено
  - `участники` теперь рендерятся как `48px` rows ближе к Figma:
    - current founder row без destructive action
    - остальные участники через `Исключить`
  - danger zone переведена на reviewed delete-room layout:
    - base state `Удалить комнату`
    - confirm state with two inputs
    - `отмена`
    - `удалить комнату`
  - при этом сохранен реальный product contract:
    - rename через существующий `PATCH`
    - kick через существующий participant delete handler
    - delete room через существующий `DELETE /api/rooms/[code]`
  - settings по-прежнему остаются только у founder/owner
- Проверки после settings sync pass:
  - `rg` больше не находит legacy settings copy:
    - `delete_room()`
    - `kick()`
    - `// переименовать комнату`
    - `// участники`
    - `// danger zone`
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Следующим profile layout feedback-pass desktop bottom stack привязан к низу экрана:
  - в shared `RoomProfilePanel` нижняя группа `logout + danger zone` теперь уходит в `mt-auto` на desktop
  - дополнительный внутренний `pb-4` у panel вместе с screen `p-4` дает требуемые `32px` от нижнего края viewport
  - mobile flow-layout не менялся
- Проверки после profile bottom-anchor pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Follow-up fix after live feedback `Все еще не внизу`:
  - root cause was not in `RoomProfilePanel` itself, but in the `profile` branch of room `main`
  - the shared panel already had `mt-auto`, but `main` for `profile` was still plain `p-4`, so there was no real flex-height context for bottom anchoring
  - route and preview `main` layout for `profile` were both changed to `p-4 flex flex-col`
  - this keeps mobile flow intact while giving desktop profile the height context needed for the lower stack to pin to the viewport bottom
- Проверки после profile screen-layout fix:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Final profile stretch fix after repeated live feedback:
  - `main` flex context alone was not enough
  - the shared `RoomProfilePanel` root also needed `app-web:flex-1`, otherwise it still collapsed to content height and the logout block stayed directly under the info card
  - after adding `app-web:flex-1`, the panel can now fill the available profile screen height and let the lower stack sit at the bottom via `mt-auto`
- Проверки после final profile stretch fix:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Follow-up spacing fix for profile bottom edge:
  - root cause: room `main` already resets desktop bottom padding with `app-web:pb-0`
  - because of that, the profile screen bottom offset on desktop was coming only from `RoomProfilePanel`, and `app-web:pb-4` produced only `16px`
  - `RoomProfilePanel` desktop bottom padding was increased to `app-web:pb-8` so the final contract is now:
    - mobile `16px`
    - desktop `32px`
- Final mobile profile anchor fix:
  - mobile was still not pinned because the shared panel used `app-web:flex-1` and `app-web:mt-auto`, so the bottom-anchoring mechanism only existed on desktop
  - `RoomProfilePanel` root now uses `flex-1` on all breakpoints
  - the lower stack now uses plain `mt-auto` on all breakpoints
  - desktop still keeps the larger `app-web:pb-8`, while mobile inherits the surrounding `16px` screen padding from room `main`
- Проверки после final mobile profile anchor fix:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Profile bottom-stack spacing pass against Figma `179:7409` and `179:7401`:
  - external spacing is now controlled by the shared bottom stack itself via `flex flex-col gap-4`
  - the individual `logoutWrapper` / `leaveWrapper` cards no longer add their own `mt-4`
  - `logoutWrapper` button row keeps `pt-2` and `gap-4` for the two confirm actions
  - `leaveWrapper` button row keeps `pt-2` without unnecessary extra horizontal gap for the single-action base state
- Проверки после profile spacing pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Profile confirm button sizing pass after live measurement:
  - root cause of the `48px` row was shared `TextButton` always using `h-10`
  - a new `size=\"compact\"` variant was added to `TextButton`, using `24px` height while preserving the same typography and color contract
  - profile logout/leave rows now use compact text buttons
  - leave confirm row again uses `gap-4`, so the two actions no longer overlap
- Проверки после profile confirm button sizing pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Theme toggle icon-swap animation pass:
  - `IconButton` now supports an optional two-icon swap contract via `alternateIcon` + `alternateActive`
  - the swap uses the same mounted-both-icons approach as the password visibility toggle:
    - `opacity`
    - `scale(0.25) -> scale(1)`
    - `blur(4px) -> blur(0)`
  - `ThemeToggle` now uses this contract for `dark_mode` <-> `light_mode`
  - all other icon buttons keep the old single-icon behavior unless the new props are provided
- Проверки после theme toggle icon animation pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/room/FR2LAG` -> `307 /login` (expected for unauthenticated HTTP probe)
- Room header icon animation pass:
  - the room switcher toggle now uses the shared `IconButton` alternate-icon swap for `expand_more` <-> `expand_less`
  - the room-code copy control now keeps both `content_copy` and `check` icons mounted and swaps them with the same `opacity + scale + blur` contract
  - the copied state still keeps the green `check` color
- Проверки после room header icon animation pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/room/FR2LAG` -> `307 /login` (expected for unauthenticated HTTP probe)
- Leaderboard streak pass against Figma `178:4176`:
  - `LeaderboardParticipant` now includes `streakDays`
  - room API now computes current streak from unique session dates
  - streak counts only days with at least one saved session; because `POST /api/sessions` rejects `value <= 0`, this matches the product rule “minimum +1 repetition”
  - current streak is anchored to today:
    - if there is no qualifying session today -> `0`
    - otherwise it counts backward across consecutive days
  - shared `RoomLeaderboard` caption now renders:
    - `сессий: N · лучшая: X · стрик: Y день/дня/дней`
  - preview/dev mocks were extended with `streakDays` so `/design-preview` stays in parity
- Проверки после leaderboard streak pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Settings refinement pass against Figma `179:8174`:
  - participant rows now use the reviewed content contract:
    - lowercase `исключить`
    - confirm-state `да, исключить` / `нет, оставить`
    - owner label `создатель комнаты`
  - exclusion is no longer immediate on first click; the row switches into a local confirm-state first
  - the settings screen now anchors the danger zone through layout, not ad hoc margins:
    - participants table is the `flex-1` section
    - the table section keeps `56px` bottom breathing room before the danger zone
    - the danger zone is the final shrink-0 block
    - bottom edge contract remains:
      - mobile `16px`
      - desktop `32px`
  - base danger action copy is now lowercase `удалить комнату`
  - when exclusion is confirmed, the local pending row state is cleared before the real kick handler runs
- Проверки после settings refinement pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Password typography pass for shared `Input`:
  - first attempt with `WebkitTextSecurity` was not enough, because the browser still drew native password dots and `IBM Plex Mono` did not visually participate
  - final fix: hidden password state in shared `Input` now renders a local overlay of real `*` glyphs on top of a transparent text input
  - the overlay uses `Font Family/Secondary` (`IBM Plex Mono`), so the masked state now follows the actual project typography contract
  - this fixes the same contract everywhere the shared `Input` is used:
    - room settings delete confirmation
    - `/login`
    - `/register`
- Проверки после final password typography pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Settings delete-confirm autoscroll pass:
  - after clicking `удалить комнату`, the shared `RoomSettingsPanel` now scrolls the revealed danger-zone confirm block into view
  - the scroll is triggered only when the delete confirm state opens
  - because the behavior lives in the shared panel, it now applies to both the real room route and the preview surface
- Проверки после settings delete-confirm autoscroll pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Settings delete-confirm validation timing pass:
  - root cause: both delete-confirm inputs were receiving `caption` immediately from empty values, so validation appeared as soon as the form opened
  - `RoomSettingsPanel` now tracks a local `deleteAttempted` flag
  - confirm-field captions are shown only after the first failed submit attempt
  - closing the confirm state resets the flag, so reopening starts clean again
- Проверки после settings delete-confirm validation timing pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Settings rename-field contract pass:
  - the save button copy is now lowercase `сохранить`
  - real `/room/[code]` no longer opens the rename field as placeholder-only
  - `renameValue` is now synchronized with the current `room.name` without clobbering dirty edits during normal screen usage
  - after a successful rename, the field keeps the updated room name as its real value instead of clearing to an empty state
- Проверки после settings rename-field contract pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Profile copy localization pass:
  - route and preview role labels are now Russian:
    - `создатель комнаты`
    - `участник`
  - logout text buttons in the shared `RoomProfilePanel` were normalized to lowercase:
    - `выйти из профиля`
    - `да, выйти`
    - `нет, остаться`
- Проверки после profile copy localization pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Settings mobile delete-confirm action-order pass:
  - the previous fix still left two issues:
    - desktop button order was accidentally flipped
    - mobile autoscroll still did not keep enough safe space above the sticky tabbar
  - final action-row contract is now:
    - base layout `flex-col-reverse`
    - `app-web:flex-row`
    - DOM order remains `отмена` then `удалить комнату`
    - result:
      - desktop -> `отмена` left, `удалить комнату` right
      - mobile -> `удалить комнату` top, `отмена` bottom
  - the mobile scroll target now has explicit `scroll-margin-bottom`, so the confirm row is scrolled above the sticky tabbar instead of into it
- Проверки после final settings delete-confirm action/layout pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Follow-up mobile action-order adjustment:
  - the latest feedback clarified that on mobile the confirm buttons should stack in the opposite order from the previous pass
  - action row base layout now uses plain `flex-col`
  - final result:
    - mobile -> `отмена` on top, `удалить комнату` below
    - desktop -> `отмена` left, `удалить комнату` right
- Проверки после follow-up mobile action-order adjustment:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Settings owner-label typography pass:
  - `создатель комнаты` in `SettingsRow` no longer uses the smaller `14/22` secondary-label sizing
  - the owner label now follows the reviewed body-sm contract:
    - `Font Family/Primary`
    - `16px / 24px`
    - `font-weight: 400`
    - `letter-spacing: 0`
    - `text-secondary` color
- Проверки после settings owner-label typography pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Settings delete-confirm danger validation pass:
  - follow-up product clarification changed the contract:
    - the delete action should stay disabled until the room name matches exactly and the password field is non-empty
    - password correctness itself should only be validated server-side after submit
  - `RoomSettingsPanel` now uses that clarified contract directly:
    - no eager client-side field error state for delete confirm
    - `удалить комнату` button is disabled until the client-known prerequisites are met
    - after enable, wrong password is handled by the server response
- Проверки после clarified settings delete gating pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Password-manager compatibility rollback:
  - the custom hidden-password masking workaround was removed from the shared `Input`
  - hidden password state now uses the native browser mask again via real `type="password"`
  - this restores password-manager recognition and password generation flows on:
    - `/register`
    - `/login`
    - room settings delete confirm
  - visible password state still switches to `type="text"` and keeps the existing animated eye-toggle behavior
- Проверки после password-mask rollback:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
- Settings wrong-password field-validation pass:
  - backend already returned `Неверный пароль` for room deletion, but the real route stored that message only in the generic `actionError`
  - `RoomSettingsPanel` did not receive a delete-specific error prop, so the password input showed neither caption nor danger border on wrong-password responses
  - the route now stores a dedicated `deletePasswordError`
  - the shared settings panel maps that message directly to the password input:
    - `caption`
    - `error`
  - the wrong-password field state is cleared on password edit and when the confirm form closes
- Проверки после settings wrong-password field-validation pass:
  - `npx tsc --noEmit` -> OK
  - `curl -I http://127.0.0.1:3000/design-preview` -> `200 OK`
